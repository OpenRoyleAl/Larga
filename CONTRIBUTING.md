# Contributing

PRs against `main`. Student copy: **Larga**, **Ai** (not AI), **CloudFlare**, **Profile**, **Cebu Ai Agent Cup**. Born in Cebu.

Secrets: `npx wrangler secret put` only. Never commit keys. Shared Cup hostname is Agent OpenRoyleAl running `npm run deploy:cup`, not a `routes` block in `wrangler.jsonc`.

```sh
npm test
```
